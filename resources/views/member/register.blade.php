@extends('layouts.app') @section('content')<section class="shell max-w-2xl py-12"><p class="text-xs font-black tracking-widest text-brand">MEMBERSHIP</p><h1 class="mt-2 text-4xl font-black">สมัครสมาชิก</h1><p class="mt-2 text-black/60 dark:text-white/60">ไม่มีรหัสผ่าน ใช้เพียงเบอร์โทรศัพท์ในการค้นหาข้อมูล</p><form class="card mt-7 grid gap-5 sm:grid-cols-2" method="POST" action="{{ route('members.store') }}">@csrf @foreach(['first_name'=>'ชื่อ','last_name'=>'นามสกุล','phone'=>'เบอร์โทรศัพท์','birth_date'=>'วันเกิด'] as $field=>$label)<div><label class="label">{{ $label }}</label><input name="{{ $field }}" type="{{ $field === 'birth_date' ? 'date' : 'text' }}" value="{{ old($field) }}" required>@error($field)<p class="mt-1 text-xs text-brand">{{ $message }}</p>@enderror</div>@endforeach<div><label class="label">เพศ</label><select name="gender" required><option value="">เลือกเพศ</option><option value="male">ชาย</option><option value="female">หญิง</option><option value="other">อื่น ๆ</option></select></div><div><label class="label">อีเมล <span class="font-normal text-black/40">(ไม่บังคับ)</span></label><input name="email" type="email" value="{{ old('email') }}"></div><button class="btn btn-primary sm:col-span-2">สมัครสมาชิก</button></form></section>@endsection

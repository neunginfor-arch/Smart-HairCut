@extends('layouts.app')
@section('content')
<div class="shell pt-8"><a class="btn border border-black/15 dark:border-white/20" href="{{ route('admin.dashboard') }}">← Admin Dashboard</a></div>
<section class="shell py-8"><div class="rounded-3xl bg-ink p-7 text-white"><p class="text-xs font-black tracking-[.2em] text-red-400">BOOKING OPERATIONS</p><div class="mt-3 flex flex-wrap items-end justify-between gap-4"><div><h1 class="text-4xl font-black">จัดการคิว</h1><p class="mt-2 text-sm text-white/60">Check-in ลูกค้า แล้ว Check-out พร้อมกรอกยอดขายเพื่อเพิ่มคะแนน</p></div><div class="rounded-xl bg-white/10 px-4 py-3 text-sm"><b>{{ number_format($bookings->total()) }}</b> รายการทั้งหมด</div></div></div><div class="card mt-7 overflow-x-auto p-0"><table class="w-full min-w-[1080px] text-left text-sm"><thead class="border-b border-black/10 bg-black/[.03] text-xs text-black/50 dark:border-white/10 dark:bg-white/5 dark:text-white/50"><tr><th class="p-4">Booking No.</th><th class="p-4">ลูกค้า</th><th class="p-4">บริการ / ช่าง</th><th class="p-4">วันเวลา</th><th class="p-4">สถานะ</th><th class="p-4">จัดการ</th></tr></thead><tbody>@forelse($bookings as $booking)<tr class="border-b border-black/5 align-top transition hover:bg-black/[.02] dark:border-white/5 dark:hover:bg-white/5"><td class="p-4 font-black">{{ $booking->booking_no }}</td><td class="p-4">{{ $booking->member->first_name }} {{ $booking->member->last_name }}<br><span class="text-xs text-black/50 dark:text-white/50">{{ $booking->member->phone }}</span></td><td class="p-4">{{ $booking->service->name }}<br><span class="text-xs text-black/50 dark:text-white/50">{{ $booking->employee->name }}</span></td><td class="p-4">{{ $booking->booking_date->format('d/m/Y') }}<br><span class="font-bold">{{ substr($booking->start_time,0,5) }}</span></td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs font-black {{ $booking->status==='completed' ? 'bg-emerald-100 text-emerald-700' : ($booking->status==='cancelled' ? 'bg-red-100 text-brand' : ($booking->status==='checked_in' ? 'bg-blue-100 text-blue-700' : 'bg-amber-100 text-amber-900')) }}">{{ $booking->status }}</span></td><td class="p-4">@if(in_array($booking->status,['pending','confirmed']))<div class="flex gap-2"><form method="POST" action="{{ route('admin.bookings.check-in',$booking) }}">@csrf<button class="btn bg-emerald-500 px-3 py-2 text-black">Check-in</button></form><form method="POST" action="{{ route('admin.bookings.cancel',$booking) }}" onsubmit="return confirm('ยืนยันการยกเลิกคิว?')">@csrf<button class="btn border border-red-300 px-3 py-2 text-brand">ยกเลิกคิว</button></form></div>@elseif($booking->status==='checked_in')<form class="flex items-center gap-2" method="POST" action="{{ route('admin.bookings.check-out',$booking) }}">@csrf<input class="w-28 px-3 py-2" type="number" name="sales_amount" min="0" step="0.01" placeholder="ยอดขาย" required><button class="btn btn-primary px-3 py-2">Check-out</button></form><p class="mt-2 text-xs text-black/50 dark:text-white/50">ทุก 100 บาท = 10 คะแนน</p>@else<span class="text-xs text-black/45 dark:text-white/45">ดำเนินการเสร็จสิ้น</span>@endif</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-black/45">ยังไม่มีรายการจอง</td></tr>@endforelse</tbody></table><div class="p-5">{{ $bookings->links() }}</div></div></section>
@endsection
